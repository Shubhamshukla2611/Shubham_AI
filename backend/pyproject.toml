[project]
name = "ai-persona-chatbot-backend"
version = "0.1.0"
description = "RAG-powered persona chatbot backend"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py310"
